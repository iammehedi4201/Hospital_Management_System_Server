import { IPatient } from '../Patient/Patient.interface';
import { Patient } from '../Patient/Patient.model';

const CreatePatientToDB = async (payLoad: IPatient) => {};

export const PatientService = {
  CreatePatientToDB,
};
